---
/**
 * Redirect: /edit/[slug] -> /editor/[slug]
 * Esta página ha sido reemplazada por el editor unificado.
 */
export const prerender = false;

const { slug } = Astro.params;

if (!slug) {
  return Astro.redirect('/editor', 301);
}

return Astro.redirect(`/editor/${slug}`, 301);
---
